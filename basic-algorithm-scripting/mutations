function mutation(arr) {
  var split1= arr[0].toLowerCase().split("");
  var split2= arr[1].toLowerCase().split(""); 
  for(var i = 0; i < arr[1].length; i++) {
  if (split1.indexOf(split2[i]) === -1) {
    return false;
  } 
  }
 return true;
       
}
mutation(["hello", "hey"]);
