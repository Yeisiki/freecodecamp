function repeatStringNumTimes(str, num) {
  // repeat after me
   var negNum = "";
  if (num >= 0) { 
  return str.repeat(num);
  } else if (num < 0) {
  return negNum;
}
}

repeatStringNumTimes("abc", -2);
